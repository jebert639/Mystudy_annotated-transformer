# ============================================================
# Annotated Transformer (Chinese annotated edition) - Requirements
# Python 3.9 recommended (torch 1.11.0+cu113 wheels support up to 3.10)
# ============================================================

# ---------- Core: training / inference / GUI (required) ----------
--find-links https://download.pytorch.org/whl/torch_stable.html
torch==1.11.0+cu113      # GPU build (CUDA 11.3); for CPU-only use torch==1.11.0
tiktoken==0.9.0          # gpt2 BPE tokenizer (50257 vocab + 3 special tokens)
matplotlib==3.9.2        # training curve plots (Agg backend)
numpy==1.26.4            # matplotlib dependency, verified with the combo above
# The inference GUI uses tkinter from the Python standard library (no extra install)

# ---------- Original Annotated Transformer build (optional) ----------
# Only needed to reproduce the original notebook / blog page
# (make notebook / make html). Not used by training or inference.
pandas==1.3.5
torchdata==0.3.0
torchtext==0.12
spacy==3.2
altair==4.1
jupytext==1.13
wandb
GPUtil

# ---------- Development tools (optional) ----------
flake8                   # static checks (CI / make flake)
black                    # code formatting (make black)
